---
benchmark_path: "tech-en/content/en/docs/tasks/configure-pod-container/projected-service-account-token.md"
benchmark_basename: "Projected service account token"
benchmark_folder: "tech-en/content/en/docs/tasks/configure-pod-container"
benchmark_headings: "Projected token"
benchmark_aliases: "projected service account token pod"
benchmark_tags: "projected token"
---

# Projected service account token

## Projected token

projected service account token setup shows how a pod mounts projected credentials for runtime access
- 中文补充
  - 场景: release / compatibility / upgrade
  - path hint: configure-pod-container/projected-service-account-token.md
  - shared anchors: plugin compatibility rollout migration
---
title: "Projected service account token"
aliases: ["projected service account token pod"]
tags: ["projected token"]
---

# Projected service account token

> [!note]
> path hint: `configure-pod-container/projected-service-account-token.md`
> heading hint: **Projected token**

- overview
  - [[Projected service account token]]
  - [ref](configure-pod-container/projected-service-account-token.md.md)
  - inline code: `Projected token`
  - tasks
    - [ ] revisit ranking note
    - [x] keep markdown noise in corpus

1. capture note
2. compare aliases
3. check nested bullets

| field | value |
| --- | --- |
| path | configure-pod-container/projected-service-account-token.md |
| alias | projected service account token pod |

```md
- nested bullet for configure-pod-container/projected-service-account-token.md
  - alias: projected service account token pod
  - tag: projected token
```

Text with **bold**, _italic_, ~~strike~~, and `inline-code` markers.

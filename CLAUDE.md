# 项目协作约定

## Token 消耗测试审批规则

- 凡是会调用外部模型 API 并消耗 token 的测试、benchmark、A/B 验证、回归验证，都必须先获得用户明确批准后才能执行。
- 在获得批准前，默认只允许执行纯本地验证，包括：
  - dry-run 预算估算
  - 离线 benchmark
  - 本地代理实验
  - 不调用 API 的单元测试、集成测试、构建检查
- 在发起审批前，必须提前说明：
  - 测试目标
  - 测试方法
  - 预计 token 消耗区间
  - 是否存在更省 token 的替代方案
  - 不同预算对应的置信度差异
- 默认优先选择“在保证结论置信度的前提下更省 token”的方案。
- 即使本地配置中已经存在可用的 API key，也不能直接执行会消耗 token 的测试。
- 若用户未明确批准，则视为禁止执行相关 API 测试。
